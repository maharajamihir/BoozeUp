import { StyleSheet } from 'react-native';

export const textStyles = StyleSheet.create({
    title: {
        paddingBottom: 30,
        fontSize: 30,
        fontWeight: "bold"
      },
      paragraph:{
        fontSize: 20,
      }

});
